<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pmp.mapper.AuthMapper">

    <!-- 手动定义结果映射 -->
    <resultMap id="UserResultMap" type="com.pmp.domain.auth.UserDO">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="fullName" column="full_name"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findUserById" parameterType="java.lang.Long" resultMap="UserResultMap">
        SELECT id,
               username,
               password,
               email,
               phone,
               full_name,
               status,
               create_time,
               update_time
        FROM sys_users
        WHERE id = #{userId}
    </select>

    <select id="findByUsername" parameterType="java.lang.String" resultMap="UserResultMap">
        SELECT id,
               username,
               password,
               email,
               phone,
               full_name,
               status,
               create_time,
               update_time
        FROM sys_users
        WHERE username = #{username}
    </select>

</mapper>